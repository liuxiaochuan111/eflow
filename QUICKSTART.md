# EFlow 快速启动指南

## 前置要求

- Node.js >= 16.x
- npm >= 8.x

## 启动步骤

### 1. 安装依赖

```bash
# 安装后端依赖
cd backend
npm install

# 安装前端依赖
cd ../frontend
npm install
```

### 2. 启动后端服务

```bash
# 在 backend 目录下
npm run dev
```

后端服务将在 http://localhost:3001 启动

### 3. 启动前端服务

打开新的终端窗口：

```bash
# 在 frontend 目录下
npm run dev
```

前端服务将在 http://localhost:3000 启动

### 4. 访问应用

在浏览器中打开：http://localhost:3000

## 使用说明

### 创建表单

1. 访问表单清单页面
2. 点击"新增表单"按钮
3. 进入编排界面

### 拖拽编排

1. 从左侧面板选择"容器"或"表单项"标签
2. 将组件卡片拖拽到中间画布
3. 画布会高亮显示可放置的区域
4. 松开鼠标完成放置

### 配置组件

1. 点击画布中的组件
2. 在右侧"属性"面板配置组件属性
3. 在右侧"事件"面板配置事件联动

### 预览与保存

- 点击"预览"按钮查看表单效果
- 点击"保存"按钮保存表单配置

## 组件说明

### 容器组件

- **Block** - 区块容器，基础容器类型
- **Container** - 通用容器，可嵌套其他组件
- **Form** - 表单容器，提供表单布局
- **Row** - 栅格容器，支持响应式布局

### 表单项组件

- **Input** - 输入框
- **InputNumber** - 数字输入框
- **Select** - 选择器
- **DatePicker** - 日期选择器
- **TimePicker** - 时间选择器
- **Radio** - 单选框
- **Checkbox** - 复选框
- **Switch** - 开关
- **Slider** - 滑块
- **Upload** - 上传
- **Cascader** - 级联选择器
- **Textarea** - 文本域

## 拖拽提示

- 拖拽时，目标容器会显示蓝色高亮边框
- 可放置的位置会显示绿色插入线
- 松开鼠标即可完成放置
- 点击组件可以查看和编辑属性

## 开发说明

### 目录结构

```
eflow/
├── frontend/          # 前端项目
│   ├── src/
│   │   ├── components/   # 组件
│   │   ├── composables/  # 组合式函数
│   │   ├── configs/      # 配置清单
│   │   ├── types/        # 类型定义
│   │   ├── utils/        # 工具函数
│   │   └── views/        # 页面
├── backend/           # 后端项目
└── docs/              # 文档
```

### 添加新组件

1. 在 `configs/form-items.ts` 添加组件配置
2. 创建对应的渲染组件（可选）
3. 在 `components/index.ts` 注册组件

### 调试技巧

- 打开浏览器开发者工具查看网络请求
- 使用 Vue DevTools 调试组件状态
- 查看控制台日志了解拖拽事件

## 常见问题

**Q: 拖拽组件没有反应？**

A: 确保后端服务已启动，前端控制台没有错误信息。

**Q: 保存表单失败？**

A: 检查后端服务是否正常运行，查看网络请求是否成功。

**Q: 组件显示异常？**

A: 清除浏览器缓存，重新启动前端服务。

## 技术支持

如有问题，请查看：
- [README.md](../README.md)
- [使用指南.md](../docs/使用指南.md)
- [API文档.md](../docs/API文档.md)
