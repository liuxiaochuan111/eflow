# EFlow 功能测试指南

## 启动前检查

### 1. 检查依赖安装

```bash
# 运行检查脚本
node check-deps.js

# 或手动检查
cd frontend && ls node_modules
cd ../backend && ls node_modules
```

### 2. 启动服务

```bash
# 终端 1 - 启动后端
cd backend
npm run dev

# 终端 2 - 启动前端
cd frontend
npm run dev
```

### 3. 访问应用

打开浏览器访问: http://localhost:3000

## 功能测试清单

### ✅ 基础功能测试

#### 1. 表单清单页面
- [ ] 页面正常加载
- [ ] 显示示例表单列表（Mock 数据）
- [ ] "新增表单"按钮可点击
- [ ] "编辑"按钮可点击
- [ ] "删除"按钮有确认提示

#### 2. 拖拽功能测试

**测试步骤：**
1. 点击"新增表单"进入编排页面
2. 从左侧"容器"标签拖拽一个 Block 组件到中间画布
3. 观察效果：
   - [ ] 拖拽时组件卡片浮起
   - [ ] 拖拽时目标容器显示蓝色高亮
   - [ ] 松开鼠标后组件成功添加到画布
4. 继续拖拽其他容器测试：
   - [ ] Container 组件
   - [ ] Form 组件
   - [ ] Row 组件

**表单项拖拽测试：**
1. 切换到左侧"表单项"标签
2. 拖拽一个 Input 组件到画布上的容器中
3. 观察效果：
   - [ ] 拖拽时显示绿色插入线
   - [ ] 松开鼠标后表单项成功添加
4. 测试其他表单项：
   - [ ] Select 组件
   - [ ] DatePicker 组件
   - [ ] Switch 组件

#### 3. 组件选择和配置

**组件选择测试：**
1. 点击画布中已添加的组件
2. 观察效果：
   - [ ] 组件边框变为蓝色
   - [ ] 顶部工具栏显示"已选择"标签
   - [ ] 右侧面板显示组件属性

**属性配置测试：**
1. 选中一个表单项组件
2. 在右侧"属性"面板修改属性：
   - [ ] 修改"字段名"（model）
   - [ ] 修改"标签"文字
   - [ ] 切换"必填"开关
   - [ ] 切换"显示"开关
3. 观察组件实时更新

#### 4. 组件删除功能

**删除测试：**
1. 选中一个组件
2. 点击组件上的"删除"按钮
3. 观察效果：
   - [ ] 显示确认提示（如果有）
   - [ ] 组件从画布中移除
   - [ ] 右侧面板清空（如果删除的是选中组件）

#### 5. 预览功能

**预览测试：**
1. 添加一些表单项组件
2. 点击顶部"预览"按钮
3. 观察效果：
   - [ ] 弹出预览对话框
   - [ ] 表单以只读模式显示
   - [ ] 可以填写表单数据
   - [ ] 关闭对话框返回编辑模式

#### 6. 保存功能

**保存测试：**
1. 编排一个简单的表单
2. 点击顶部"保存"按钮
3. 观察效果：
   - [ ] 显示"保存成功"提示
   - [ ] 表单 ID 自动生成
4. 刷新页面，验证数据持久化

#### 7. 重置功能

**重置测试：**
1. 在编排页面添加一些组件
2. 点击顶部"重置"按钮
3. 观察效果：
   - [ ] 显示"已重置表单"提示
   - [ ] 画布清空
   - [ ] 所有组件被移除

### ✅ 高级功能测试

#### 8. 容器嵌套测试

**嵌套测试：**
1. 拖拽一个 Block 容器
2. 在 Block 中拖拽一个 Form 容器
3. 在 Form 中拖拽一个 Row 容器
4. 在 Row 中添加表单项
5. 观察效果：
   - [ ] 所有层级正确显示
   - [ ] 每个容器都有对应的标签
   - [ ] 拖拽插入指示器正确显示

#### 9. 左侧结构树测试

**结构树测试：**
1. 在画布上添加多个组件
2. 切换到左侧"结构"标签
3. 观察效果：
   - [ ] 显示完整的组件树
   - [ ] 树形结构正确反映嵌套关系
   - [ ] 点击树节点可以选中对应组件

#### 10. 组件卡片样式测试

**视觉测试：**
1. 查看左侧组件面板
2. 检查效果：
   - [ ] 所有组件卡片显示正确
   - [ ] 图标使用渐变色背景
   - [ ] 悬停时有浮起效果
   - [ ] 显示组件中英文名称

### ✅ 边界情况测试

#### 11. 空状态测试

**空状态测试：**
1. 创建新表单（不添加任何组件）
2. 观察画布：
   - [ ] 显示空状态提示
   - [ ] 提示文字清晰友好
3. 拖拽组件到空容器：
   - [ ] 空状态提示消失
   - [ ] 组件正常添加

#### 12. 快速操作测试

**快速连续操作：**
1. 快速连续拖拽多个组件
2. 快速切换选中不同组件
3. 快速修改属性
4. 观察效果：
   - [ ] 没有崩溃或错误
   - [ ] 操作响应及时
   - [ ] 状态同步正确

## 常见问题排查

### 问题 1: 拖拽不生效

**检查步骤：**
1. 打开浏览器控制台（F12）
2. 查看是否有 JavaScript 错误
3. 确认后端服务是否正常运行（端口 3001）
4. 清除浏览器缓存重试

### 问题 2: 按钮点击无反应

**检查步骤：**
1. 确认是否选中了组件
2. 查看控制台是否有事件日志
3. 检查组件是否正确渲染

### 问题 3: 页面显示异常

**检查步骤：**
1. 确认前端和后端都已启动
2. 检查浏览器控制台错误
3. 尝试硬刷新（Ctrl + Shift + R）

### 问题 4: 保存失败

**检查步骤：**
1. 确认后端服务正在运行
2. 检查网络请求是否成功
3. 查看后端控制台日志

## 性能检查

### 1. 页面加载速度
- [ ] 首次加载时间 < 3 秒
- [ ] 组件渲染流畅，无明显卡顿

### 2. 拖拽性能
- [ ] 拖拽时没有明显延迟
- [ ] 高亮效果响应及时
- [ ] 可以流畅拖拽多个组件

### 3. 内存使用
- [ ] 长时间使用没有内存泄漏
- [ ] 页面响应保持流畅

## 测试报告模板

完成测试后，可以按以下格式报告问题：

```
### 问题报告

**问题描述**：[简要描述问题]

**复现步骤**：
1. [步骤 1]
2. [步骤 2]
3. [步骤 3]

**预期结果**：[应该发生什么]

**实际结果**：[实际发生了什么]

**环境信息**：
- 浏览器：[Chrome/Firefox/Safari 等]
- 操作系统：[Windows/Mac/Linux]
- Node.js 版本：[node -v]
```

## 测试完成标准

所有核心功能测试通过 ✅：
- 拖拽功能正常
- 配置功能正常
- 删除功能正常
- 预览功能正常
- 保存功能正常

---

**提示**: 测试过程中如有问题，请查看浏览器控制台和后端日志，这些信息有助于快速定位问题。
