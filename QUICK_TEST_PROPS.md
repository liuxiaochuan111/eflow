# 属性面板显示修复 - 快速测试指南

## 修复内容

已修复右侧属性面板只显示5个字段的问题，现在可以显示所有258个属性。

## 快速测试

### 1. 启动项目
```bash
cd frontend
npm run dev
```

### 2. 测试 Input 组件的完整属性

1. 打开浏览器访问 http://localhost:3000
2. 点击"新增表单"进入编排页面
3. 拖拽一个 **Input（输入框）**组件到画布
4. 点击选中该组件
5. 查看右侧面板

**预期效果：**
- 看到4个分类标签页：基础属性、显示属性、组件属性、样式属性
- 点击"组件属性"标签
- 看到徽章 `25` 表示有25个可配置属性
- 滚动查看所有属性：

**基础属性标签页：**
- ✓ 标签文本
- ✓ 字段名（model）
- ✓ 栅格占位
- ✓ 是否必填

**显示属性标签页：**
- ✓ 显示状态
- ✓ 是否禁用

**组件属性标签页：**
```
✓ 占位文本
✓ 可清空
✓ 最大长度
✓ 显示字数统计
✓ 输入框类型 (text/textarea/password/number/email/url/search/tel)
✓ 前缀图标
✓ 后缀图标
✓ 只读
✓ 自动聚焦
✓ 最小长度
✓ 正则表达式
✓ 触发验证
... 共25个属性
```

**样式属性标签页：**
- ✓ 自定义类名
- ✓ 内联样式
- ✓ 宽度

### 3. 测试其他组件

#### Select 选择器（19个属性）
1. 拖拽 Select 组件到画布
2. 点击选中
3. 切换到"组件属性"标签
4. 看到徽章 `19`

#### DatePicker 日期选择器（14个属性）
1. 拖拽 DatePicker 组件
2. 查看所有14个属性配置

#### Switch 开关（17个属性）
1. 拖拽 Switch 组件
2. 查看所有17个属性配置

## 对比效果

### 修复前
```
右侧属性面板：
├─ 显示 (Switch)
├─ 字段名 (Input)
├─ 标签 (Input)
├─ 必填 (Switch)
└─ 自定义样式 (Textarea)

共：5个字段 ❌
```

### 修复后
```
右侧属性面板：
├─ 基础属性 (4个)
├─ 显示属性 (2个)
├─ 组件属性 (25个) ✅
│  ├─ 占位文本
│  ├─ 可清空
│  ├─ 最大长度
│  ├─ 显示字数统计
│  ├─ 输入框类型
│  ├─ 前缀图标
│  ├─ 后缀图标
│  ├─ 只读
│  ├─ 自动聚焦
│  ├─ 最小长度
│  ├─ 正则表达式
│  ├─ 触发验证
│  └─ ... 更多属性
└─ 样式属性 (3个)

共：34个可配置项 ✅
```

## 所有组件属性数量

| 组件 | 属性数 | 徽章显示 |
|------|--------|---------|
| Input | 25 | `25` |
| InputNumber | 14 | `14` |
| Select | 19 | `19` |
| DatePicker | 14 | `14` |
| TimePicker | 14 | `14` |
| Radio | 10 | `10` |
| Checkbox | 17 | `17` |
| Switch | 17 | `17` |
| Slider | 20 | `20` |
| Upload | 16 | `16` |
| Cascader | 21 | `21` |
| Rate | 14 | `14` |
| ColorPicker | 9 | `9` |
| Transfer | 13 | `13` |
| TreeSelect | 25 | `25` |

## 实时预览

修改任何属性后，画布上的组件会立即更新显示效果。

### 示例1：修改 Input 组件
1. 修改 `placeholder` 为 "请输入用户名"
2. 画布上的输入框占位符立即更新

### 示例2：修改 Switch 组件
1. 修改 `activeText` 为 "启用"
2. 修改 `inactiveText` 为 "禁用"
3. 修改 `activeColor` 为 "#67c23a"
4. Switch 组件立即显示新的文字和颜色

## 属性分类说明

### 基础属性
所有组件都有的核心配置：
- label（标签文本）
- model（字段名）
- span（栅格占位，仅表单项）
- required（是否必填，仅表单项）

### 显示属性
控制组件的显示状态：
- display（显示/隐藏）
- disabled（禁用/启用）

### 组件属性
每个组件特有的所有配置，例如：
- Input：输入类型、图标、验证等
- Select：多选、搜索、远程数据等
- DatePicker：日期类型、格式化等

### 样式属性
自定义外观：
- className（CSS类名）
- style（内联样式）
- width（宽度）

## 常见问题

### Q1: 为什么有些属性看不到？
A: 不同组件有不同的属性，查看标签页上的徽章数字。

### Q2: 属性修改后没有效果？
A: 点击组件确保选中状态，或尝试刷新页面。

### Q3: 如何快速找到某个属性？
A: 使用浏览器的查找功能（Ctrl+F）在属性面板中搜索。

## 验证清单

完成以下测试确认修复成功：

- [ ] Input 组件显示25个组件属性
- [ ] Select 组件显示19个组件属性
- [ ] Switch 组件显示17个组件属性
- [ ] 修改属性后组件实时更新
- [ ] 属性分类标签页正常工作
- [ ] 属性数量徽章正确显示

## 技术细节

- 组件配置文件：`frontend/src/configs/form-items.ts`
- 属性面板组件：`frontend/src/components/panels/PropsConfigPanel.vue`
- 使用方式：`Builder.vue` 中引用 `PropsConfigPanel`

所有258个属性现在都可以在右侧面板中配置了！🎉
